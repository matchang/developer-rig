// Display name validation errors
export const NAME_EMPTY = 'Name must not be empty';
export const NAME_CHAR_LIMIT = 'Name must be less than 256 characters';

// SKU validation errors
export const SKU_EMPTY = 'SKU must not be empty';
export const SKU_CHAR_LIMIT = 'SKU must not contain any whitespace';
export const SKU_WHITESPACE = 'SKU must be less than 256 characters';
export const SKU_UNIQUE = 'SKU must be unique';

// Amount validation errors
export const AMOUNT_EMPTY = 'Amount must not be empty';
export const AMOUNT_OUT_OF_RANGE = 'Amount must be between 1 and 10,000';